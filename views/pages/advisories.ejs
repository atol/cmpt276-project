<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css"
        integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" crossorigin="anonymous">

    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <link rel="stylesheet" href="./stylesheets/styleHomePage.css">
    <link rel="stylesheet" href="./stylesheets/advisories.css">

    <title>Ditu - Travel Advisories</title>
</head>

<body>
    <nav class="navbar navbar-expand-md">
        <a href="/" class="navbar-brand">DITU</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#main-navigation"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse menu" id="main-navigation">
            <ul class="navbar-nav">
                <li class="nav-item active">
                    <a href="/" class="nav-link">Home</a>
                </li>
                <% if (logged_in) { %>
                    <li class="nav-item active">
                        <a href="/dashboard" class="nav-link">Profile</a>
                    </li>
                <li class="nav-item active">
                    <a href="/logout" class="nav-link">Log Out</a>
                </li>
                <% } else { %>
                <li class="nav-item active">
                    <a href="/login" class="nav-link">Log In</a>
                </li>
                <li class="nav-item active">
                    <a href="/signup" class="nav-link">Sign up</a>
                </li>
                <% } %>
            </ul>
        </div>
    </nav>

    <div class="container">
        <h1>Travel Advisories</h1>
        <table class="advisory">
            <tr>
                <th style="width: 400px">Destination</th>
                <th>Advisory</th>
                <th style="width: 200px">Last Updated</th>
            </tr>
            <% results.forEach(function(r) { %>
            <tr>
                <td><%= r.country %></td>
                <td><%= r.advisory %></td>
                <td><%= r.updated %></td>
            </tr>
            <% }); %>
        </table>
    </div>
</body>

</html>